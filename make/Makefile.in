TEX = $(filter-out $(AUX), $(shell ls *.tex))
PDF = $(TEX:%.tex=%.pdf)
PDFLATEX = pdflatex -interaction nonstopmode -file-line-error

all: $(PDF)

$(PDF): %.pdf: %.tex pre
	$(PDFLATEX) $<  || $(RM) $@

echo:
	@echo $(PDF)
